class prog4
{
    public static void main(String []args)
    {
        int a,b,c,max;
        a=Integer.parseInt(args[10]);
        b=Integer.parseInt(args[20]);
        c=Integer.parseInt(args[30]);
        max=(a>b)?((a>c)?a:c):((b>c)?b:c);
        System.out.println("Max=> "+max);
    }
}

